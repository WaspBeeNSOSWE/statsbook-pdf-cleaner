# TODO

## Screenshots for README

README.md has four `<!-- SCREENSHOT-N -->` placeholders. Replace each with a real image once available. Suggested location: `screenshots/` folder in this repo.

### Screenshot 1 — Before / After (most important)
- **Left:** Excel print preview of a statsbook with all 15 sheet tabs visible at the bottom, and the filename/date printed in the footer.
- **Right:** The clean PDF produced by the script — 8 sheets, no footer clutter.

### Screenshot 2 — Installation verified
- Terminal window showing `python3 --version` (e.g. Python 3.11.x) and `soffice --version` (e.g. LibreOffice 7.x) both returning valid output.

### Screenshot 3 — Folder layout
- Finder (or Explorer / Nautilus) window showing the parent folder with `crg-scoreboard_v2025.8` and `statsbook-pdf-cleaner` side by side.

### Screenshot 4 — Successful run
- Terminal output from a real run. Blur or change team names if the game should stay private.

---

## Other ideas (not urgent)

- GUI wrapper for non-technical NSOs
- Windows and Linux real-world testing
- Handle non-default CRG folder layouts
- English translation could be improved by a native English-speaking derby NSO
